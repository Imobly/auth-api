# ============================================
# Auth API - Single .env (DEV/HML/PROD)
# ============================================
// How to use:
// 1) Set ENVIRONMENT to: development | staging | production
// 2) Fill DATABASE_URL_DEV / DATABASE_URL_HML / DATABASE_URL_PROD
// 3) Optionally set DATABASE_URL to force a specific target
// 4) SECRET_KEY must match Backend for the same environment

# Environment selector
ENVIRONMENT=development

# Database URLs (selected by ENVIRONMENT)
DATABASE_URL_DEV=postgresql://postgres:admin123@postgres:5432/auth_db
DATABASE_URL_HML=postgresql://postgres.yyeldattafklyutbbnhu:[YOUR-PASSWORD]@aws-0-us-west-2.pooler.supabase.com:6543/postgres
DATABASE_URL_PROD=postgresql://postgres.[PROJECT-ID]:[PASSWORD]@aws-0-REGION.pooler.supabase.com:6543/postgres
# Force URL (overrides selector)
# DATABASE_URL=

# SSL mode and schema
DB_SSLMODE=require
DB_SCHEMA=auth_api

# Security (must match Backend in the same environment)
SECRET_KEY=replace-me-with-secure-key
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# CORS (comma-separated list for frontend domains)
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# Server
HOST=0.0.0.0
PORT=8000
DEBUG=true
