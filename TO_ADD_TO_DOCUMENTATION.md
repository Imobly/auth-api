Copiar o conteúdo deste arquivo para o repositório `Documentation` (pasta de sua escolha).

O arquivo `docs/AUTH_DOCUMENTATION.md` contém a documentação completa da API de autenticação.

Passos sugeridos:

1. No repositório `Documentation`, criar uma pasta `auth` ou `services/auth`.
2. Copiar `AUTH_DOCUMENTATION.md` para lá.
3. Fazer commit e push.

Se quiser, eu posso criar um PR nesse repositório se você me fornecer a URL do repositório `Documentation` e permissões (ou posso gerar um patch que você aplique manualmente).
